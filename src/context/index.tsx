// import { useContract, useContractWrite } from "@thirdweb-dev/react";
// import React, { ReactFragment, useContext } from "react";

// const initialValues = {

// }

// export const ContractContext = useContext();

// export const ContractProvider = ({children} : {children : React.ReactNode}) =>{
//     const {contract} = useContract(process.env.CONTRACT_ADDRESS);

//     const {mutateAsync : createCampaign} = useContractWrite(contract, 'createCampaign');
// }